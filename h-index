class Solution:
    def hIndex(self, citations: List[int]) -> int:
        citations.sort()
        x=citations[::-1]
        h=0
        for i in range(len(citations)):
            if x[i]>=i+1:
                h=i+1
            else:
                break
        return h
